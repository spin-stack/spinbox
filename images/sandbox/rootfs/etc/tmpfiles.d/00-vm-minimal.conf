# Minimal tmpfiles configuration for fast VM boot
# Only create essential runtime directories

# Essential runtime directories
d /run/lock 0755 root root -
d /run/sshd 0755 root root -
d /tmp 1777 root root -

# Skip X11 and unnecessary directories (override system defaults)
# These are created as empty overrides to prevent processing
